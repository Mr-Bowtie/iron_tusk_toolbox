<%= form_with(model: pull_batch) do |form| %>
  <% if pull_batch.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pull_batch.errors.count, "error") %> prohibited this pull_batch from being saved:</h2>

      <ul>
        <% pull_batch.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
