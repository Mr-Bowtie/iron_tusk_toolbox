<%= form_with(model: inventory_card) do |form| %>
  <% if inventory_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_card.errors.count, "error") %> prohibited this inventory_card from being saved:</h2>

      <ul>
        <% inventory_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
