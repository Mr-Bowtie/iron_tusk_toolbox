<%= form_with(model: inventory_location) do |form| %>
  <% if inventory_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_location.errors.count, "error") %> prohibited this inventory_location from being saved:</h2>

      <ul>
        <% inventory_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
