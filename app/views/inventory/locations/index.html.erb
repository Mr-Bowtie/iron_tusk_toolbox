<p style="color: green"><%= notice %></p>

<% content_for :title, "Locations" %>

<h1>Locations</h1>

<div id="inventory_locations">
  <% @inventory_locations.each do |inventory_location| %>
    <%= render inventory_location %>
    <p>
      <%= link_to "Show this location", inventory_location %>
    </p>
  <% end %>
</div>

<%= link_to "New location", new_inventory_location_path %>
